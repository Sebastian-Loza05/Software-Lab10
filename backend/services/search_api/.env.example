# Search API gateway. Orchestrates the 3 downstream services in parallel.
# All values have sensible localhost defaults; override only if needed.
# POKE_API_URL=http://localhost:8001
# POKE_STATS_URL=http://localhost:8002
# POKE_IMAGES_URL=http://localhost:8003
# DOWNSTREAM_TIMEOUT=5
