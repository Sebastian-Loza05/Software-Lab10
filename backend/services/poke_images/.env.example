# POKE Images serves flat {name}.jpg files from a local folder that stands in
# for the File Server / S3 in the architecture diagram. By default it reads
# <project>/data/images (populated by scripts/ingest_images.py); set IMAGES_DIR
# only to point at a different location.
# IMAGES_DIR=/absolute/path/to/images
